@model List<Personne>
@{
    //Layout = "~Views/Shared/_SpecialLayout.cshtml";
    //Layout = "_SpecialLayout";
    ViewData["title"] = "Liste des contacts";
}

<div>
    @if(ViewBag.Message != null) {
        <div class="alert alert-success">
            @ViewBag.Message
        </div>
    }
    <h1>Liste des contacts</h1>
    @*@foreach(Personne p in ViewData["contacts"] as List<Personne>)
    {
            <div>Nom : @p.LastName, Prénom : @p.FirstName, Email: @p.Email</div>
    }*@
    @foreach(Personne p in Model)
    {
            <div>Nom : @p.LastName, Prénom : @p.FirstName, Email: @p.Email <a asp-controller="Contact" asp-action="Detail" asp-route-id="@p.Id">Détail</a></div>
            @*@foreach(Adresse a in p.Adresses) {
                <div>
                    @a.Street @a.City @a.PostalCode
                </div>
            }*@
    }
</>